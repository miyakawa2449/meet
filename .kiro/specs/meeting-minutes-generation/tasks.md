# タスク一覧

## Phase 6: 議事録生成機能

### Task 23: データモデルと設定

**ステータス**: 未着手

**説明**: 議事録用のデータモデルを追加し、既存モデルを拡張する。

**サブタスク**:
1. `models.py`に`MinutesConfig`データクラスを追加
2. `models.py`に`ActionItem`データクラスを追加
3. `models.py`に`Decision`データクラスを追加
4. `models.py`に`Topic`データクラスを追加
5. `models.py`に`MeetingMinutes`データクラスを追加
6. `PipelineConfig`を議事録関連フィールドで拡張
7. `Timing`データクラスに`minutes_sec`フィールドを追加

**受け入れ基準**:
- すべての新しいデータクラスが適切に型付けされている
- すべてのフィールドに適切なデフォルト値がある
- データクラスが`models.py`からインポート可能

**変更ファイル**:
- `src/meeting_pipeline/models.py`

---

### Task 24: 議事録生成コアモジュール

**ステータス**: 未着手

**説明**: 議事録生成のコアロジックを実装する。

**サブタスク**:
1. `src/meeting_pipeline/minutes.py`を作成
2. `_load_meeting_json()`関数を実装
3. `_prepare_prompt()`関数を実装
4. リトライロジック付きの`_call_openai_api()`を実装
5. `_parse_api_response()`関数を実装
6. メイン関数`generate_minutes()`を実装
7. 包括的なエラーハンドリングを追加
8. すべてのステージにログを追加

**受け入れ基準**:
- `generate_minutes()`がMeeting JSONパスを受け取り`MeetingMinutes`を返す
- OpenAI APIが適切なリトライロジックで呼び出される（3回リトライ、指数バックオフ）
- APIレスポンスが構造化データにパースされる
- エラーが適切にキャッチされログに記録される
- すべての関数にdocstringがある

**作成ファイル**:
- `src/meeting_pipeline/minutes.py`

**依存関係**:
- Task 23（データモデル）

---

### Task 25: トークン管理とチャンク化

**ステータス**: 未着手

**説明**: 長い文字起こしのためのトークン推定とチャンク化を実装する。

**サブタスク**:
1. `_estimate_tokens()`関数を実装
2. `_handle_chunking()`関数を実装
3. `_merge_chunked_results()`関数を実装
4. トークン推定のテストを追加
5. チャンク化ロジックのテストを追加

**受け入れ基準**:
- トークン推定が合理的に正確（±20%）
- トークン制限を超える場合、文字起こしがチャンクに分割される
- チャンクが境界でコンテキストを保持する
- マージされた結果が一貫している

**変更ファイル**:
- `src/meeting_pipeline/minutes.py`

**依存関係**:
- Task 24（コアモジュール）

---

### Task 26: 議事録出力生成

**ステータス**: 未着手

**説明**: 議事録のMarkdownとJSON出力生成を実装する。

**サブタスク**:
1. `output.py`に`generate_minutes_markdown()`を実装
2. `output.py`に`save_minutes_markdown()`を実装
3. `output.py`に`save_minutes_json()`を実装
4. 議事録用のタイムスタンプフォーマットを追加
5. アクションアイテム用のテーブルフォーマットを追加
6. 適切なMarkdown構造を追加（見出し、リスト、テーブル）

**受け入れ基準**:
- Markdown出力が整形され読みやすい
- JSON出力が有効でラウンドトリップ可能
- タイムスタンプが一貫してフォーマットされている（HH:MM:SS）
- アクションアイテムがテーブル形式で表示される
- すべてのセクションが適切に構造化されている

**変更ファイル**:
- `src/meeting_pipeline/output.py`

**依存関係**:
- Task 23（データモデル）

---

### Task 27: CLI統合

**ステータス**: 未着手

**説明**: 議事録生成用のコマンドラインオプションを追加する。

**サブタスク**:
1. CLIに`--generate-minutes`引数を追加
2. CLIに`--minutes-model`引数を追加
3. CLIに`--minutes-language`引数を追加
4. `PipelineConfig`パースを更新
5. OPENAI_API_KEYの検証を追加
6. ヘルプテキストを更新

**受け入れ基準**:
- 新しい引数が正しくパースされる
- `--generate-minutes`が設定されている場合、APIキーの検証チェックがある
- ヘルプテキストが明確で有益
- デフォルト値が適切

**変更ファイル**:
- `src/meeting_pipeline/cli.py`

**依存関係**:
- Task 23（データモデル）

---

### Task 28: パイプライン統合

**ステータス**: 未着手

**説明**: 議事録生成をメインパイプラインに統合する。

**サブタスク**:
1. `pipeline.py`にステージ8（議事録生成）を追加
2. 環境からOPENAI_API_KEYを読み込む
3. Meeting JSON保存後に`generate_minutes()`を呼び出す
4. 議事録出力（MarkdownとJSON）を保存
5. タイミング測定を更新
6. エラーハンドリングを追加（議事録失敗でパイプラインを失敗させない）
7. 議事録生成ステージのログを追加

**受け入れ基準**:
- 議事録生成がMeeting JSON保存後に実行される
- 議事録生成が失敗してもパイプラインは継続する
- タイミングが`timing.minutes_sec`に記録される
- エラーがログに記録されユーザーに報告される
- Meeting JSONと文字起こしは常に最初に保存される

**変更ファイル**:
- `src/meeting_pipeline/pipeline.py`

**依存関係**:
- Task 24（コアモジュール）
- Task 26（出力生成）
- Task 27（CLI統合）

---

### Task 29: 議事録モジュールのユニットテスト

**ステータス**: 未着手

**説明**: 議事録生成の包括的なユニットテストを作成する。

**サブタスク**:
1. `tests/test_minutes.py`を作成
2. `_load_meeting_json()`のテストを追加
3. `_prepare_prompt()`のテストを追加
4. `_call_openai_api()`のテストを追加（モック使用）
5. `_parse_api_response()`のテストを追加
6. リトライロジックのテストを追加
7. エラーハンドリングのテストを追加
8. トークン推定のテストを追加
9. チャンク化のテストを追加（実装されている場合）

**受け入れ基準**:
- すべてのコア関数がテストされている
- OpenAI API呼び出しがモックされている
- エラーシナリオがカバーされている
- テストが一貫して合格する
- `minutes.py`のテストカバレッジ > 80%

**作成ファイル**:
- `tests/test_minutes.py`

**依存関係**:
- Task 24（コアモジュール）
- Task 25（チャンク化）

---

### Task 30: 出力生成のユニットテスト

**ステータス**: 未着手

**説明**: 議事録出力関数のユニットテストを作成する。

**サブタスク**:
1. `tests/test_meeting_pipeline.py`に`generate_minutes_markdown()`のテストを追加
2. `save_minutes_markdown()`のテストを追加
3. `save_minutes_json()`のテストを追加
4. Markdownフォーマットのテストを追加
5. JSONラウンドトリップ検証のテストを追加

**受け入れ基準**:
- Markdown出力が検証されている
- JSON出力が検証されている
- ラウンドトリップテストが合格する
- エッジケースがカバーされている（空リスト、欠落フィールド）

**変更ファイル**:
- `tests/test_meeting_pipeline.py`

**依存関係**:
- Task 26（出力生成）

---

### Task 31: 統合テスト

**ステータス**: 未着手

**説明**: 議事録生成のエンドツーエンド統合テストを作成する。

**サブタスク**:
1. `--generate-minutes`フラグ付きのE2Eテストを追加
2. 統合テスト用にOpenAI APIをモック
3. すべての出力ファイルが生成されることを検証
4. 議事録コンテンツ構造を検証
5. エラーハンドリングをテスト（APIキー欠落、API失敗）

**受け入れ基準**:
- E2Eテストが議事録生成付きで完全なパイプラインを実行する
- すべての出力が検証される（Meeting JSON、文字起こしMD、議事録MD、議事録JSON）
- エラーシナリオがテストされる
- テストがモックAPIを使用する（CIで実際のAPI呼び出しなし）

**変更ファイル**:
- `tests/test_meeting_pipeline.py`または`tests/test_e2e_minutes.py`を作成

**依存関係**:
- Task 28（パイプライン統合）

---

### Task 32: ドキュメント更新

**ステータス**: 未着手

**説明**: Phase 6のドキュメントを更新する。

**サブタスク**:
1. `src/README.md`に「議事録生成」セクションを追加
2. `--generate-minutes`オプションを文書化
3. `--minutes-model`オプションを文書化
4. `--minutes-language`オプションを文書化
5. 使用例を追加
6. OPENAI_API_KEYセットアップを文書化
7. トラブルシューティングセクションを追加
8. 機能リストを更新

**受け入れ基準**:
- READMEに議事録生成の明確な手順がある
- 使用例が提供されている
- APIキーセットアップが文書化されている
- トラブルシューティングのヒントが含まれている

**変更ファイル**:
- `src/README.md`

**依存関係**:
- Task 28（パイプライン統合）

---

### Task 33: 手動テストと検証

**ステータス**: 未着手

**説明**: 実際のOpenAI APIで手動テストを実施する。

**サブタスク**:
1. 短い会議（5分）でテスト
2. 中程度の会議（30分）でテスト
3. 長い会議（1時間以上）でテスト
4. 異なるモデル（gpt-3.5-turbo、gpt-4）でテスト
5. 異なる言語（日本語、英語）でテスト
6. 出力品質を検証
7. パフォーマンスを検証（1時間の会議で60秒未満）

**受け入れ基準**:
- すべてのテストケースが合格する
- 出力品質が許容できる
- パフォーマンスが要件を満たす
- エラーや警告がない

**依存関係**:
- Task 28（パイプライン統合）
- Task 32（ドキュメント）

---

### Task 34: 最終チェックポイントとブログ記事準備

**ステータス**: 未着手

**説明**: 最終検証とブログ記事用コンテンツの準備。

**サブタスク**:
1. すべてのユニットテストを実行（目標: 70+テスト合格）
2. 議事録生成付きでE2Eテストを実行
3. すべてのドキュメントが最新であることを確認
4. ブログ記事用のサンプル出力を作成
5. 出力のスクリーンショットを撮る
6. パフォーマンスメトリクスを測定
7. ブログ記事のアウトラインを準備

**受け入れ基準**:
- すべてのテストが合格する（70+テスト）
- ドキュメントが完全
- サンプル出力が生成されている
- パフォーマンスメトリクスが記録されている
- ブログ記事のアウトラインが準備できている

**依存関係**:
- すべての前のタスク

---

## タスクサマリー

| タスク | 説明 | ステータス | 依存関係 |
|------|------|--------|---------|
| 23 | データモデルと設定 | 未着手 | - |
| 24 | 議事録生成コアモジュール | 未着手 | 23 |
| 25 | トークン管理とチャンク化 | 未着手 | 24 |
| 26 | 議事録出力生成 | 未着手 | 23 |
| 27 | CLI統合 | 未着手 | 23 |
| 28 | パイプライン統合 | 未着手 | 24, 26, 27 |
| 29 | 議事録モジュールのユニットテスト | 未着手 | 24, 25 |
| 30 | 出力生成のユニットテスト | 未着手 | 26 |
| 31 | 統合テスト | 未着手 | 28 |
| 32 | ドキュメント更新 | 未着手 | 28 |
| 33 | 手動テストと検証 | 未着手 | 28, 32 |
| 34 | 最終チェックポイントとブログ記事準備 | 未着手 | すべて |

**総タスク数**: 12

**推定時間**: 10-12時間

---

## 実装順序

### フェーズ1: 基盤（Task 23-24）
1. Task 23: データモデル
2. Task 24: コアモジュール

### フェーズ2: 統合（Task 25-28）
3. Task 25: チャンク化（オプション、延期可能）
4. Task 26: 出力生成
5. Task 27: CLI統合
6. Task 28: パイプライン統合

### フェーズ3: テスト（Task 29-31）
7. Task 29: ユニットテスト（議事録）
8. Task 30: ユニットテスト（出力）
9. Task 31: 統合テスト

### フェーズ4: ドキュメントと検証（Task 32-34）
10. Task 32: ドキュメント
11. Task 33: 手動テスト
12. Task 34: 最終チェックポイント

---

## 注意事項

- Task 25（チャンク化）は時間が限られている場合は延期可能 - ほとんどの会議はトークン制限内に収まる
- まずコア機能に集中（Task 23-28）
- テストは段階的に追加可能（Task 29-31）
- 手動テスト（Task 33）には実際のOpenAI APIキーが必要
- ブログ記事準備（Task 34）はすべての実装完了後に実施可能
